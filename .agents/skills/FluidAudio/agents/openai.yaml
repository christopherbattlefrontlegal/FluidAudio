interface:
  display_name: "FluidAudio"
  short_description: "Repo-specific patterns and workflows for FluidAudio"
  default_prompt: "Use the FluidAudio repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true